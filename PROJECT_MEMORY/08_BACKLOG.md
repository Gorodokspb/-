# Backlog

Lower-priority but remembered work:
- continue incremental UI polish only where it removes real workflow friction;
- review whether any repeated path-repair logic can be centralized safely;
- document contract generation behavior more explicitly once the live flow is re-verified;
- prepare the shared cash desk implementation in small safe steps rather than one big refactor.

Not backlog, but guardrails:
- large cleanup of `CRM.py` for style alone is not a current goal;
- rewriting estimate storage architecture is not a current goal.
